Dear Team,

#Docker run -it --name dockercont1 ubuntu /bin/bash
#Docker run -it --name dockercont2 -v /kanha ubuntu /bin/bash
#docker run -it --name dockercont3 --volumes-from dockercont2 ubuntu /bin/bash
